# 🎓 Система управления студентами

Веб-приложение для управления заявками студентов и военным учётом с локальным хранением данных.

## ✨ Возможности

- 📝 Заявки студентов с формой заполнения
- 🪖 Военный учёт (личные карточки)
- 📊 Импорт/Экспорт данных в Excel
- 💾 Локальное хранение (IndexedDB)
- 📄 Генерация PDF документов
- 🔍 Поиск и фильтрация
- ✏️ Редактирование записей

## 🚀 Быстрый старт (локально)

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск проекта

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

## 📦 Развертывание на Vercel

### 1. Подготовка репозитория
- Зайдите в Supabase Dashboard → SQL Editor
- Скопируйте содержимое `src/lib/database.sql`
- Выполните скрипт

### 4. Запуск проекта

#### Вариант 1: Веб-версия (рекомендуется для разработки)

```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

#### Вариант 2: Electron приложение (десктопная версия)

```bash
npm run electron:dev
```

## 📋 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера (веб-версия) |
| `npm run build` | Сборка проекта для production |
| `npm run electron:dev` | Запуск Electron приложения в режиме разработки |
| `npm run electron:build` | Сборка Electron приложения |
| `npm run electron:pack` | Упаковка Electron приложения |
| `npm run lint` | Проверка кода линтером |

## 👤 Создание первого пользователя

После настройки базы данных:

1. Зарегистрируйтесь через Supabase Auth:
   - Зайдите в Supabase Dashboard → Authentication
   - Создайте пользователя вручную или используйте Email/Password провайдер

2. Назначьте роль в таблице `profiles`:
   ```sql
   UPDATE profiles 
   SET role = 'admin' 
   WHERE email = 'your-email@example.com';
   ```

## 🎯 Роли пользователей

### Администратор
- Полный CRUD доступ ко всем сущностям
- Управление группами, студентами, занятиями
- Просмотр всех отчетов

### Преподаватель
- Создание и редактирование своих занятий
- Отметка посещаемости для своих занятий
- Просмотр отчетов по своим группам

## 📁 Структура проекта

```
├── src/
│   ├── components/     # React компоненты (.tsx)
│   ├── views/          # Vue компоненты (.vue)
│   ├── services/       # API сервисы
│   ├── stores/         # Pinia stores
│   ├── router/         # Vue Router
│   ├── lib/            # Конфигурация Supabase и SQL
│   ├── types/          # TypeScript типы
│   └── utils/          # Утилиты (экспорт в Excel/CSV)
├── electron/           # Electron конфигурация
└── public/             # Статические файлы
```

## 🔧 Требования

- **Node.js**: >= 18.0.0
- **npm**: >= 9.0.0
- **Supabase**: Аккаунт и проект на supabase.com

## ⚠️ Важные замечания

1. **Смешанный стек**: Проект использует Vue 3 (основной) + React компоненты (.tsx)
2. **База данных**: Используется Supabase (облачная) или better-sqlite3 (локальная для Electron)
3. **Первый запуск**: Обязательно настройте `.env` файл с ключами Supabase

## 🐛 Решение проблем

### Ошибка подключения к Supabase
- Проверьте правильность ключей в `.env`
- Убедитесь, что проект в Supabase активен

### Ошибки при установке better-sqlite3
```bash
npm install --build-from-source better-sqlite3
```

### Порт 5173 занят
Измените порт в `vite.config.ts`:
```ts
server: {
  port: 3000, // или другой свободный порт
}
```

## 📞 Поддержка

При возникновении проблем проверьте:
1. Все зависимости установлены (`npm install`)
2. Файл `.env` создан и заполнен
3. База данных настроена (SQL скрипт выполнен)
4. Node.js версия >= 18








