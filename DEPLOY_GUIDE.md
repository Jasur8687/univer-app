# 🚀 Деплой на Vercel через GitHub

## ✅ Подготовка завершена!

Все лишние файлы удалены:
- ❌ Старые фото (.jpg)
- ❌ TypeScript компоненты (.tsx)
- ❌ Supabase файлы (api.ts, supabase.ts)
- ❌ Старая документация (DATABASE_SETUP.md, requirements.txt)
- ❌ SQL файлы (database.sql)

## 📋 Что готово:

✅ `vercel.json` - конфигурация Vercel
✅ `.gitignore` - обновлен
✅ `package.json` - все зависимости
✅ IndexedDB - локальное хранение
✅ Excel импорт/экспорт
✅ PDF генерация

## 🔧 Шаги для деплоя:

### 1. Инициализировать Git (если еще не сделано)

```bash
git init
git add .
git commit -m "Initial commit: Student Management System"
```

### 2. Создать репозиторий на GitHub

1. Зайти на https://github.com
2. Нажать **New repository**
3. Название: `student-management-system` (или любое)
4. Описание: `Система управления студентами с локальным хранением`
5. **НЕ** инициализировать README, .gitignore (уже есть)
6. Создать репозиторий

### 3. Подключить локальный репозиторий к GitHub

```bash
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

### 4. Деплой на Vercel

#### Вариант A: Через Vercel Dashboard (рекомендуется)

1. Зайти на https://vercel.com
2. Нажать **Add New Project**
3. Выбрать **Import Git Repository**
4. Выбрать ваш GitHub репозиторий
5. **Framework Preset**: Vite (автоматически определится)
6. **Build Command**: `npm run build` (уже заполнено)
7. **Output Directory**: `dist` (уже заполнено)
8. Нажать **Deploy**

#### Вариант B: Через Vercel CLI

```bash
# Установить Vercel CLI
npm install -g vercel

# Войти в аккаунт
vercel login

# Деплой
vercel
```

### 5. Настройки проекта в Vercel

После первого деплоя:

1. Settings → General
   - **Project Name**: можно изменить
   - **Domain**: `your-project-name.vercel.app`

2. Settings → Git
   - Автоматический деплой при push в main

## 🌐 Результат

После деплоя получите URL вида:
```
https://your-project-name.vercel.app
```

## ⚙️ Технические детали

### Что происходит при деплое:

1. Vercel клонирует репозиторий
2. Запускает `npm install`
3. Запускает `npm run build`
4. Деплоит папку `dist`
5. Настраивает CDN и сертификат SSL

### Особенности:

- ✅ **IndexedDB работает в браузере** - данные хранятся локально
- ✅ **Нет серверной части** - все работает на клиенте
- ✅ **Быстрая загрузка** - статические файлы
- ✅ **Автоматические обновления** - при push в GitHub

## 🔄 Обновление приложения

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Vercel автоматически задеплоит новую версию!

## 📝 Полезные команды

```bash
# Локальная разработка
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр сборки
npm run preview

# Проверка на ошибки
npm run lint
```

## 🎯 Структура проекта

```
/
├── src/
│   ├── components/      # Vue компоненты
│   ├── pages/          # Страницы приложения
│   ├── lib/            # IndexedDB
│   ├── utils/          # Excel импорт/экспорт
│   ├── stores/         # Vuex store
│   ├── router/         # Vue Router
│   └── assets/         # Стили
├── public/             # Статические файлы
├── dist/              # Сборка (не в Git)
├── vercel.json        # Конфигурация Vercel
├── package.json       # Зависимости
└── vite.config.js     # Конфигурация Vite
```

## 🆘 Возможные проблемы

### Проблема: Build failed
**Решение:** Проверьте package.json и запустите `npm install` локально

### Проблема: Белый экран после деплоя
**Решение:** Проверьте консоль браузера (F12), возможно проблема с путями

### Проблема: IndexedDB не работает
**Решение:** IndexedDB работает только в браузере, не на сервере (это нормально!)

## 📚 Документация

- `INDEXEDDB_GUIDE.md` - Руководство по локальной БД
- `UPDATE_NOTES.md` - История обновлений
- `FORMS_GUIDE.md` - Руководство по формам

## 🎉 Готово!

Теперь можно:
1. Запушить в GitHub
2. Подключить к Vercel
3. Получить рабочее приложение онлайн

---

**Создано:** 10 декабря 2025 г.
